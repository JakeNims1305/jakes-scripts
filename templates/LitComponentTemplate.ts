import { css, CSSResultOrNative, html, HTMLTemplateResult, LitElement } from "lit";
import { IREPLACE_ME_1 } from "./REPLACE_ME_2.interface"
import { REPLACE_ME_1Styles } from "./REPLACE_ME_2.styles";

export class REPLACE_ME_1 extends LitElement implements IREPLACE_ME_1 {
  static override get styles(): CSSResultOrNative[] {
    return [
      REPLACE_ME_1Styles,
      css``
    ];
  }

  // Lifecycle Methods -------------------------------------------------------//
  override connectedCallback(): void {
    super.connectedCallback();
  }

  override disconnectedCallback(): void {
    super.disconnectedCallback();
  }

  // Render ------------------------------------------------------------------//
  override render(): HTMLTemplateResult {
    return html``;
  }
}
